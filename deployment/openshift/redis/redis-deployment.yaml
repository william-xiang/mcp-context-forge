apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
    spec:
      containers:
        - name: redis
          image: redis:8.4.0
          ports:
            - containerPort: 6379
